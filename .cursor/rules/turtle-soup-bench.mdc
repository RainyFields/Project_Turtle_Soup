---
description: turtle-soup-bench 项目记忆与开发约定（新开对话请先读 AGENTS.md）
alwaysApply: true
---

# turtle-soup-bench — Project Memory

## What this repo is

海龟汤双 Agent 评测：Oracle + Questioner。代号 `turtle-soup-bench`。

## Status (2026-08-31)

- ✅ 题集重建完成：`real/` **22 题**，全部人工审核，欠定度标注 100%
- ✅ 工具链就绪：Oracle 审计、可跑性预检、网格驱动、逐轮量记录
- 🔶 **全量网格未跑** —— 这是当前的主线任务
- 🔲 论文实验章节已清空待数据（`docs/paper/iab2026-draft.md` 是内容真源）

## 接手后第一件事

读 `plan.md` →「下一阶段」，然后按那四条命令跑。
不要从下面的 Entry points 开始 —— 那些是离线自检。

## Entry points

> ⚠️ 下面带 `--mock` 或单题的命令是**离线自检**，不是实验。
> 正式重跑走 `plan.md` →「下一阶段 → 怎么跑」的四条命令。

- `python scripts/run_game.py --puzzle refsoup_008 --mock`
- `python scripts/run_pilot.py --puzzles refsoup_008 --mock`
- `python scripts/run_benchmark.py --puzzles refsoup --mock`
- `AGENTS.md` · `plan.md` · `generator/README.md`

## Code map

- `engine/game.py` · `evaluation/round_studies.py` · `evaluation/judge.py`
- Providers: `agents/model_providers/`（含 `openrouter_provider.py`）
- Puzzles: `data/puzzles/real/` **22 题**（站点收录，经两轮人工审核）+ `generated/` 9 题（禁用）
- 取题一律 `list_puzzle_ids(family="real")`；`"all"`/`"turtle"`/`"refsoup"` 会跨目录
- Default test puzzle: **`refsoup_008`**（沙漠里的尸体，旧编号 refsoup_006）

## 换模型时必读（AGENTS.md「跑实验前的两条硬规则」有完整说明）

- **Oracle / 裁判必须与 Questioner 不同家族**。同家族共享训练数据与表述习惯，
  同家族的提问更易被「听懂」，会把「规模效应」和「与 Oracle 的相似度」绑死。
  已知问题：693 局网格里 Qwen3.5-397B 同时是 Questioner、Oracle 和裁判，重跑须换。
  当前异构选择：`z-ai/glm-5.3-flash`（`qwen-flash` 准确率同为 90% 但仍属 Qwen 家族）。
- **先审计再跑**：`python scripts/audit_oracle.py --provider <p> --model <m>`，
  通过线 ≥90%。弱模型会把答不出的问题全塌陷成「与此无关」（零信息），
  30% 的 Oracle 在 30 轮只传 8.4 bits < 识别正解所需的 ~10 bits —— 理想 Questioner 也解不出。
- **推理模型两个坑**：思考 token 算进 `max_tokens`/`num_predict` 预算，给小了会返回
  **空 content**（静默错答）；思考还会让延迟涨 ~100 倍。
- 本地 Ollama：`.env` 设 `OLLAMA_THINK=0`。

## 重跑 693 局 = 完全重跑

题集（11→22，零重叠）、Oracle 家族、锚点三处同时变更，**论文 §5 每个数字都需重建，
新旧不可比**。重跑须：两个锚点都算、保存逐轮步长与距离、`qa_rounds` 入库
（否则核心图画不出来且 E3 无法复核）。脚本已内置这些护栏，详见 `plan.md`。

## 题目难度（＝原「深度 × 广度」讨论的结论）

- 原想分深度/广度两个维度，实测两者都撞天花板且本身不独立，**已合并为一个量**：
  说「题目难度」「欠定度」「深度广度」指的是同一件事
- 用**欠定度**（`scripts/annotate_puzzle_dimensions.py` → `data/puzzles/dimensions.json`）
- **不要**用 puzzle JSON 的 `difficulty` 字段（来自站点评分＝好不好玩）
  或 `composite_judge.difficulty_band`（只按线索条数分档）——两者对同一批题给出相反分布
- 难度必须与「所需轮数」独立，否则 accuracy-vs-rounds 的分组图会自证

## 不要运行这两个（会破坏题库）

- `scripts/refresh_reference_key_clues.py` —— 旧词典抽取器，会把现有线索
  覆盖成碎片词。用 `scripts/refresh_key_clues_llm.py`
- `scripts/import_reference_puzzles.py --replace` —— 删光题库重导，
  两轮人工审核作废。增补题目用 `--external-ids <站点id...>`

两者现在都会拒绝执行并提示替代方案。

## Conventions

- `--mock` unless real APIs; `pytest -q`; Python 3.10+; minimal diff.
- 密钥只进 `.env`（gitignored）；**绝不在对话/PR 里粘贴 key**。
- 新增 provider：照 `zai_provider.py` 写，接进 `provider_factory.py` + `scripts/check_env.py`。
